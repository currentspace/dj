{"timestamp":"2026-02-28T22:58:00Z","category":"typescript","title":"Use static imports, never inline import()","description":"Inline import('module').Type in type annotations and dynamic import() for eagerly-needed modules hurt readability and bypass static analysis. They also cause cross-request issues in Workers.","actionable":"Always use static import type {...} or import {...} at the top of the file. Never use import() expressions for type annotations or for modules that are needed at load time."}
